import type { FC } from 'react';
import './LogUp.scss';

interface LogUpProps {}

const LogUp: FC<LogUpProps> = () => (
  <div className="LogUp">
    LogUp Component
  </div>
);

export default LogUp;
